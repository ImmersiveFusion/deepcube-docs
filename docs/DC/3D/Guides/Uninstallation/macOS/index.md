# Uninstalling on macOS

<!-- SP-074: the Applications-folder entries below still say the currently-shipping name.
     Per R-DOCS-005, docs that quote a shipping string stay on the old
     string until the code ships the new one. Do not "fix" these to
     DeepCube ahead of the build. -->

1. Open `Applications` on your computer.
1. Find `Immersive APM` in the list of applications.
1. Drag the `Immersive APM` to the `Trash`.
1. All set!
